#include <iostream>
#include <string>
using namespace std;

int main()
{

    string s = " zaidamjadghafarahmaads";
    for (int i = 0; i < s.length(); i++)
    {
        char a = s[i];
        cout << a << endl;
    }
    // in chracter we only initiliaze one charater

    // cout << s << endl;
    return 0;
}
